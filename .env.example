# Used by domain_assistant.py and the DeepEval bonus when running real evaluations.
# Copy this file to .env, then replace the placeholder value.
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-4o-mini
# Optional: override the judge model used by bonus_framework_compare.py.
DEEPEVAL_MODEL=gpt-4o-mini
